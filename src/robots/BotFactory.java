package robots;

/**
 * Builds and returns Bot objects from various data structures.
 *
 * @author Chris
 */
public class BotFactory {

    public Bot makeBot(BotInfo info) {
        // compile the passed BotInfo into a set of actuators, sensors and metadata 
        // build a Bot with the BotInfo

        return null;
    }
}
